[project]
name = "nargila-counter"
version = "0.1.0"
description = "Telegram bot that counts hookahs by users"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["python-telegram-bot>=22.0"]
